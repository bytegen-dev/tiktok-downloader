services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
    # Uncomment to test with a volume mount for development
    # volumes:
    #   - ./dist:/app/dist
    #   - ./dist-frontend:/app/dist-frontend

